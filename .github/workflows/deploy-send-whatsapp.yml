name: Deploy send-whatsapp

on:
  push:
    branches: [master, staging]
    paths: ['apps/send-whatsapp/**']
  workflow_dispatch:
    inputs:
      force:
        description: Force deploy (skip change detection)
        type: boolean
        default: false

jobs:
  changes:
    name: Detect changes
    if: github.event_name == 'push'
    uses: ./.github/workflows/_detect-changes.yml
    with:
      package-name: msgops-send-whatsapp
    secrets: inherit

  deploy-staging:
    needs: changes
    if: |
      always() &&
      github.ref_name == 'staging' &&
      (needs.changes.outputs.affected == 'true' || github.event_name == 'workflow_dispatch')
    uses: ./.github/workflows/_deploy-cloudrun.yml
    with:
      app-name: send-whatsapp
      image-name: send-whatsapp
      docker-context: '.'
      dockerfile: apps/send-whatsapp/Dockerfile
      cloud-run-services: '[{"service": "msgops-send-whatsapp", "port": 8080}]'
      environment: staging
      registry: us-east1-docker.pkg.dev/etus-media-development-staging/msgops
    secrets:
      GCP_SA_KEY: ${{ secrets.GCP_SA_KEY_STAGING }}

  deploy-production:
    needs: changes
    if: |
      always() &&
      github.ref_name != 'staging' &&
      (needs.changes.outputs.affected == 'true' || github.event_name == 'workflow_dispatch')
    uses: ./.github/workflows/_deploy-cloudrun.yml
    with:
      app-name: send-whatsapp
      image-name: send-whatsapp
      docker-context: '.'
      dockerfile: apps/send-whatsapp/Dockerfile
      cloud-run-services: '[{"service": "msgops-send-whatsapp", "port": 8080}]'
    secrets:
      GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
