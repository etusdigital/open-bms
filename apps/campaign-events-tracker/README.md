# campaign-events-tracker

Consumes `campaign-events-tracker.campaign.tracked` events from RabbitMQ and
records campaign-level metrics for the analytics rollup.

## Run

```bash
pnpm --filter campaign-events-tracker dev   # port 3000
```
