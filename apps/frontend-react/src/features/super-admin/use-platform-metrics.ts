// TODO S4: implement when GET /platform/metrics endpoint is available
export function usePlatformMetrics() {
  return {
    totalAccounts: null as number | null,
    activeAccounts: null as number | null,
    totalUsers: null as number | null,
  };
}
