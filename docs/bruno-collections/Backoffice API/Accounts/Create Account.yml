info:
  name: Create Account
  type: http
  seq: 5

http:
  method: POST
  url: "{{backoffice-api-url}}/accounts"
  body:
    type: json
    json: |
      {
        "name": "Test Account"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
