info:
  name: Get Account
  type: http
  seq: 4

http:
  method: GET
  url: '{{backoffice-api-url}}/accounts/1'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
