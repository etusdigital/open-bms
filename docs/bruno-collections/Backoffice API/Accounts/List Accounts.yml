info:
  name: List Accounts
  type: http
  seq: 2

http:
  method: GET
  url: "{{backoffice-api-url}}/accounts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
