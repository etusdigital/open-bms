info:
  name: Search Accounts
  type: http
  seq: 3

http:
  method: GET
  url: "{{backoffice-api-url}}/accounts/search"
  params:
    - name: q
      value: "acme"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
