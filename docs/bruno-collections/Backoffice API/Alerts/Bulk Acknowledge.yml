info:
  name: Bulk Acknowledge
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{backoffice-api-url}}/alerts/bulk-acknowledge"
  body:
    type: json
    json: |
      {
        "ids": [1, 2, 3]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
