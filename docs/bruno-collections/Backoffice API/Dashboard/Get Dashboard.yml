info:
  name: Get Dashboard
  type: http
  seq: 1

http:
  method: GET
  url: '{{backoffice-api-url}}/dashboard'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
