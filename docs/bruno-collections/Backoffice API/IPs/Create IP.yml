info:
  name: Create IP
  type: http
  seq: 4

http:
  method: POST
  url: "{{backoffice-api-url}}/ips"
  body:
    type: json
    json: |
      {
        "ipAddress": "192.168.1.100",
        "provider": "sendgrid",
        "acquiredAt": "2025-01-01"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
