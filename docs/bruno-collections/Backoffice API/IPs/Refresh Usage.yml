info:
  name: Refresh Usage
  type: http
  seq: 8

http:
  method: POST
  url: "{{backoffice-api-url}}/ips/refresh-usage"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
