info:
  name: Sync IPs
  type: http
  seq: 7

http:
  method: POST
  url: "{{backoffice-api-url}}/ips/sync"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
