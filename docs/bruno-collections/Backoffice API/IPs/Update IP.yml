info:
  name: Update IP
  type: http
  seq: 5

http:
  method: PATCH
  url: '{{backoffice-api-url}}/ips/1'
  body:
    type: json
    json: |
      {
        "provider": "aws_ses",
        "acquiredAt": "2025-01-15"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
