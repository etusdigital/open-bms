info:
  name: Create Routing Event
  type: http
  seq: 3

http:
  method: POST
  url: "{{backoffice-api-url}}/internal/routing-event"
  body:
    type: json
    json: |
      {
        "alertType": "rate_limit",
        "accountId": 1,
        "pool": "test-pool",
        "description": "Test routing event"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
