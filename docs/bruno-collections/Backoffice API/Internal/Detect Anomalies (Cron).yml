info:
  name: Detect Anomalies (Cron)
  type: http
  seq: 1

http:
  method: POST
  url: "{{backoffice-api-url}}/internal/cron/detect-anomalies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
