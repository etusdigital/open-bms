info:
  name: Refresh IP Usage (Cron)
  type: http
  seq: 2

http:
  method: POST
  url: "{{backoffice-api-url}}/internal/cron/refresh-ip-usage"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
