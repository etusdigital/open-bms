info:
  name: Create Monitor Target
  type: http
  seq: 4

http:
  method: POST
  url: "{{backoffice-api-url}}/monitor-targets"
  body:
    type: json
    json: |
      {
        "name": "Test Sender Monitor",
        "targetType": "sender",
        "targetValue": "sender@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
