info:
  name: List Monitor Targets
  type: http
  seq: 2

http:
  method: GET
  url: '{{backoffice-api-url}}/monitor-targets'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
