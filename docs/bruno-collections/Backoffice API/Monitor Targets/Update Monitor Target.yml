info:
  name: Update Monitor Target
  type: http
  seq: 5

http:
  method: PATCH
  url: '{{backoffice-api-url}}/monitor-targets/1'
  body:
    type: json
    json: |
      {
        "name": "Updated Monitor",
        "enabled": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
