info:
  name: Get Automations
  type: http
  seq: 2

http:
  method: GET
  url: "{{backoffice-api-url}}/msgops/automations"
  headers:
    - name: account-id
      value: "1"
  auth:
    type: bearer
    token: "{{backoffice-token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
