info:
  name: Get Tags
  type: http
  seq: 3

http:
  method: GET
  url: '{{backoffice-api-url}}/msgops/tags'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
