info:
  name: Create Pool
  type: http
  seq: 4

http:
  method: POST
  url: "{{backoffice-api-url}}/pools"
  body:
    type: json
    json: |
      {
        "accountId": 1,
        "name": "Test Pool",
        "poolName": "test-pool",
        "senderName": "Test Sender",
        "senderEmail": "sender@example.com",
        "subuser": "test-subuser"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
