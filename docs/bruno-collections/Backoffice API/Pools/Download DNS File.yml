info:
  name: Download DNS File
  type: http
  seq: 8

http:
  method: GET
  url: "{{backoffice-api-url}}/pools/1/dns-file"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
