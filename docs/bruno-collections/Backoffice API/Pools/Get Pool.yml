info:
  name: Get Pool
  type: http
  seq: 3

http:
  method: GET
  url: "{{backoffice-api-url}}/pools/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
