info:
  name: Validate Pool
  type: http
  seq: 7

http:
  method: POST
  url: "{{backoffice-api-url}}/pools/1/validate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
