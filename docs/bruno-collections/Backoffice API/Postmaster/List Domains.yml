info:
  name: List Domains
  type: http
  seq: 1

http:
  method: GET
  url: '{{backoffice-api-url}}/postmaster/domains'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
