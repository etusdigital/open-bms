info:
  name: Get Filters
  type: http
  seq: 1

http:
  method: GET
  url: '{{backoffice-api-url}}/reports/filters'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
