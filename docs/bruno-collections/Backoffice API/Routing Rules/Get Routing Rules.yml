info:
  name: Get Routing Rules
  type: http
  seq: 1

http:
  method: GET
  url: "{{backoffice-api-url}}/routing-rules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
