info:
  name: Upsert Routing Rules
  type: http
  seq: 2

http:
  method: PUT
  url: "{{backoffice-api-url}}/routing-rules"
  body:
    type: json
    json: |
      {
        "config": {
          "rules": [],
          "exceptionRules": [],
          "defaultConfig": {}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
