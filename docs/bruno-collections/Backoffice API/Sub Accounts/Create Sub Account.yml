info:
  name: Create Sub Account
  type: http
  seq: 4

http:
  method: POST
  url: '{{backoffice-api-url}}/sub-accounts'
  body:
    type: json
    json: |
      {
        "name": "Test Sub Account",
        "description": "A test sub-account",
        "color": "#FF5733",
        "senders": [
          {
            "name": "Test Sender",
            "poolName": "test-pool",
            "senderName": "Test",
            "senderEmail": "sender@example.com",
            "subuser": "test-subuser"
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
