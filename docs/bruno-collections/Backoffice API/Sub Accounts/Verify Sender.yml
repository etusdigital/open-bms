info:
  name: Verify Sender
  type: http
  seq: 8

http:
  method: POST
  url: '{{backoffice-api-url}}/sub-accounts/1/senders/1/verify'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
