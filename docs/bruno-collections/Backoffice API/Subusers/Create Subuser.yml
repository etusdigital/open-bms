info:
  name: Create Subuser
  type: http
  seq: 4

http:
  method: POST
  url: '{{backoffice-api-url}}/subusers'
  body:
    type: json
    json: |
      {
        "username": "test-subuser",
        "ips": ["192.168.1.100"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
