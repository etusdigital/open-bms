info:
  name: Update Subuser
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{backoffice-api-url}}/subusers/test-subuser"
  body:
    type: json
    json: |
      {
        "disabled": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
