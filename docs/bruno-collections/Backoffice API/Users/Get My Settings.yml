info:
  name: Get My Settings
  type: http
  seq: 3

http:
  method: GET
  url: '{{backoffice-api-url}}/users/me/settings'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
