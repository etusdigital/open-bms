info:
  name: List Users
  type: http
  seq: 2

http:
  method: GET
  url: "{{backoffice-api-url}}/users"
  params:
    - name: page
      value: "1"
    - name: limit
      value: "50"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
