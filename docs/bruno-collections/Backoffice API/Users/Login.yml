info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{backoffice-api-url}}/users/login"
  body:
    type: json
    json: |
      {
        "email": "admin@example.com",
        "selectedAccountId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
