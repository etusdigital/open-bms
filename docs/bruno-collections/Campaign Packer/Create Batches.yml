info:
  name: Create Batches
  type: http
  seq: 3

http:
  method: POST
  url: '{{campaign-packer-url}}/create-batches'
  body:
    type: json
    json: |
      {
        "id": 1,
        "title": "Test Campaign",
        "accountId": 1
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
