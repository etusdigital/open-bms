info:
  name: Create Test
  type: http
  seq: 6

http:
  method: POST
  url: '{{campaign-packer-url}}/create-test/1'
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
