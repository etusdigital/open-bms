info:
  name: Schedule Pages
  type: http
  seq: 4

http:
  method: POST
  url: "{{campaign-packer-url}}/schedule-pages"
  body:
    type: json
    json: |
      {
        "campaignId": 1,
        "page": 1
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
