info:
  name: Warmup Start
  type: http
  seq: 8

http:
  method: POST
  url: "{{campaign-packer-url}}/warmup-start"
  body:
    type: json
    json: |
      {
        "campaignId": 1,
        "warmups": []
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
