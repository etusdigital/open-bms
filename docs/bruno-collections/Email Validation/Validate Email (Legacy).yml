info:
  name: Validate Email (Legacy)
  type: http
  seq: 2

http:
  method: GET
  url: '{{email-validation-url}}/api/validations/email'
  params:
    - name: email
      value: 'user@example.com'
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
