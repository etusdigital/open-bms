info:
  name: Receive Event
  type: http
  seq: 1

http:
  method: POST
  url: '{{event-receiver-url}}/'
  body:
    type: json
    json: |
      {
        "event": "test",
        "data": {}
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
