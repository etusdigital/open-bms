info:
  name: Conceive Lead
  type: http
  seq: 1

http:
  method: POST
  url: '{{lead-conception-url}}/'
  body:
    type: json
    json: |
      {
        "contactId": 1,
        "automationId": 1,
        "accountId": 1
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
