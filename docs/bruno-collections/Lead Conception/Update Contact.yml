info:
  name: Update Contact
  type: http
  seq: 2

http:
  method: POST
  url: '{{lead-conception-url}}/update-contact'
  body:
    type: json
    json: |
      {
        "contactId": 1,
        "accountId": 1,
        "fields": {}
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
