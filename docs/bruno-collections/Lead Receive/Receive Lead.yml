info:
  name: Receive Lead
  type: http
  seq: 1

http:
  method: POST
  url: '{{lead-receive-url}}/lead'
  body:
    type: json
    json: |
      {
        "email": "user@example.com",
        "firstName": "Test",
        "lastName": "User",
        "phone": "+5511999999999",
        "accountId": 1
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
