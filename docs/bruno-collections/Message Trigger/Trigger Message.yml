info:
  name: Trigger Message
  type: http
  seq: 2

http:
  method: POST
  url: "{{message-trigger-url}}/"
  body:
    type: json
    json: |
      {
        "contactId": 1,
        "automationId": 1,
        "stepId": 1,
        "accountId": 1
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
