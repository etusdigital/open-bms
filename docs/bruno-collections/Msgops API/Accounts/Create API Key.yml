info:
  name: Create API Key
  type: http
  seq: 11

http:
  method: POST
  url: '{{msgops-api-url}}/accounts/1/api-keys'
  body:
    type: json
    json: |
      {
        "name": "Test API Key",
        "roleCode": "viewer"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
