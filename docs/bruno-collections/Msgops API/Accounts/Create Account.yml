info:
  name: Create Account
  type: http
  seq: 5

http:
  method: POST
  url: '{{msgops-api-url}}/accounts'
  body:
    type: json
    json: |
      {
        "name": "Test Account",
        "description": "Test account description"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
