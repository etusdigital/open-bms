info:
  name: Delete Account
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{msgops-api-url}}/accounts/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
