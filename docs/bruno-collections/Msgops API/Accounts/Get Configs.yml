info:
  name: Get Configs
  type: http
  seq: 9

http:
  method: GET
  url: "{{msgops-api-url}}/accounts/configs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
