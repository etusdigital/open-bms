info:
  name: List API Keys
  type: http
  seq: 10

http:
  method: GET
  url: "{{msgops-api-url}}/accounts/1/api-keys"
  params:
    - name: page
      value: "1"
    - name: itemsPerPage
      value: "20"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
