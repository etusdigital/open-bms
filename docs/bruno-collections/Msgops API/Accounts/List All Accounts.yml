info:
  name: List All Accounts
  type: http
  seq: 3

http:
  method: GET
  url: "{{msgops-api-url}}/accounts/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
