info:
  name: Rotate API Key
  type: http
  seq: 12

http:
  method: POST
  url: "{{msgops-api-url}}/accounts/1/api-keys/1/rotate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
