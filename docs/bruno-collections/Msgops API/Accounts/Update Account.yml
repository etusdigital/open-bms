info:
  name: Update Account
  type: http
  seq: 6

http:
  method: PUT
  url: "{{msgops-api-url}}/accounts/1"
  body:
    type: json
    json: |
      {
        "name": "Updated Account"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
