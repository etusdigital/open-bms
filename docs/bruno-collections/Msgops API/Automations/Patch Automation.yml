info:
  name: Patch Automation
  type: http
  seq: 6

http:
  method: PATCH
  url: '{{msgops-api-url}}/automations/1'
  body:
    type: json
    json: |
      {
        "isActive": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
