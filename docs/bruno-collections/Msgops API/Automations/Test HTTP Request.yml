info:
  name: Test HTTP Request
  type: http
  seq: 14

http:
  method: POST
  url: '{{msgops-api-url}}/automations/http-request-test'
  body:
    type: json
    json: |
      {
        "url": "https://httpbin.org/post",
        "method": "POST",
        "headers": {},
        "body": {}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
