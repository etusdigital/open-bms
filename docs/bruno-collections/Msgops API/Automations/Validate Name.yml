info:
  name: Validate Name
  type: http
  seq: 9

http:
  method: GET
  url: "{{msgops-api-url}}/automations/validate-name"
  params:
    - name: name
      value: "test-automation"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
