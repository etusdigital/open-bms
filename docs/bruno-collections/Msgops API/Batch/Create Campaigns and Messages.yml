info:
  name: Create Campaigns and Messages
  type: http
  seq: 1

http:
  method: POST
  url: '{{msgops-api-url}}/batch/campaigns-messages'
  body:
    type: json
    json: |
      [
        {
          "title": "Test Campaign",
          "name": "test-campaign",
          "type": "email",
          "steps": []
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
