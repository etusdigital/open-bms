info:
  name: Upload Files
  type: http
  seq: 1

http:
  method: POST
  url: '{{msgops-api-url}}/buckets'
  body:
    type: json
    json: |
      [
        {
          "name": "test-file.png",
          "data": "base64-encoded-data"
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
