info:
  name: Copy Config
  type: http
  seq: 13

http:
  method: POST
  url: "{{msgops-api-url}}/campaigns-rules/configs/1/copy"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
