info:
  name: Delete Rule
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{msgops-api-url}}/campaigns-rules/rules/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
