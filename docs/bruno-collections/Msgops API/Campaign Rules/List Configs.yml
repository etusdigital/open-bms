info:
  name: List Configs
  type: http
  seq: 8

http:
  method: GET
  url: '{{msgops-api-url}}/campaigns-rules/configs'
  params:
    - name: page
      value: '1'
    - name: itemsPerPage
      value: '10'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
