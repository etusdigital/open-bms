info:
  name: List Rules
  type: http
  seq: 2

http:
  method: GET
  url: '{{msgops-api-url}}/campaigns-rules/rules'
  params:
    - name: page
      value: '1'
    - name: itemsPerPage
      value: '10'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
