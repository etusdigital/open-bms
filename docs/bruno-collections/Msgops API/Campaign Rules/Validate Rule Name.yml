info:
  name: Validate Rule Name
  type: http
  seq: 7

http:
  method: GET
  url: '{{msgops-api-url}}/campaigns-rules/rules/validate-name'
  params:
    - name: name
      value: 'test-rule'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
