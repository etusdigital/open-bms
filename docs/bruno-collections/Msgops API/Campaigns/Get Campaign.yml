info:
  name: Get Campaign
  type: http
  seq: 3

http:
  method: GET
  url: '{{msgops-api-url}}/campaigns/1'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
