info:
  name: Get Late Campaigns
  type: http
  seq: 10

http:
  method: GET
  url: '{{msgops-api-url}}/campaigns/late-campaigns'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
