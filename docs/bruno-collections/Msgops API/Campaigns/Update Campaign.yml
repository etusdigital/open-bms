info:
  name: Update Campaign
  type: http
  seq: 5

http:
  method: PUT
  url: "{{msgops-api-url}}/campaigns"
  body:
    type: json
    json: |
      {
        "id": 1,
        "title": "Updated Campaign",
        "name": "test-campaign",
        "status": "draft"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
