info:
  name: Validate Name
  type: http
  seq: 8

http:
  method: GET
  url: '{{msgops-api-url}}/campaigns/validate-name'
  params:
    - name: title
      value: 'test-campaign'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
