info:
  name: Bulk Unsubscribe
  type: http
  seq: 10

http:
  method: POST
  url: '{{msgops-api-url}}/contacts/bulk-unsubscribe'
  body:
    type: json
    json: |
      {
        "emails": ["user@example.com"],
        "date": "2025-01-01"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
