info:
  name: Count Contacts
  type: http
  seq: 5

http:
  method: GET
  url: '{{msgops-api-url}}/contacts/count'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
