info:
  name: Count Unsubscribe
  type: http
  seq: 11

http:
  method: GET
  url: '{{msgops-api-url}}/contacts/count-unsubscribe'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
