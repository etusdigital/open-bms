info:
  name: Dashboard
  type: http
  seq: 6

http:
  method: GET
  url: "{{msgops-api-url}}/contacts/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
