info:
  name: Export Contacts
  type: http
  seq: 9

http:
  method: GET
  url: "{{msgops-api-url}}/contacts/export"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
