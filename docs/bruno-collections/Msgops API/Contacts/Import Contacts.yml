info:
  name: Import Contacts
  type: http
  seq: 8

http:
  method: POST
  url: '{{msgops-api-url}}/contacts/import'
  body:
    type: json
    json: |
      [
        {
          "email": "user@example.com",
          "firstName": "Test",
          "lastName": "User",
          "phone": "+5511999999999"
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
