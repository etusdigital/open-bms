info:
  name: List Contacts
  type: http
  seq: 2

http:
  method: GET
  url: '{{msgops-api-url}}/contacts'
  params:
    - name: itemsPerPage
      value: '20'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
