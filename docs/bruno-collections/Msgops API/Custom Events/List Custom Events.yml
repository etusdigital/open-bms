info:
  name: List Custom Events
  type: http
  seq: 2

http:
  method: GET
  url: "{{msgops-api-url}}/custom-events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
