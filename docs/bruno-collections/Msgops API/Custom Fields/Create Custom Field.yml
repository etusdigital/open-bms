info:
  name: Create Custom Field
  type: http
  seq: 4

http:
  method: POST
  url: "{{msgops-api-url}}/custom-fields"
  body:
    type: json
    json: |
      {
        "name": "test_field",
        "type": "string"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
