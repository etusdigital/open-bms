info:
  name: List Custom Fields
  type: http
  seq: 2

http:
  method: GET
  url: '{{msgops-api-url}}/custom-fields'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
