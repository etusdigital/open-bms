info:
  name: Update Custom Field
  type: http
  seq: 5

http:
  method: PUT
  url: '{{msgops-api-url}}/custom-fields/1'
  body:
    type: json
    json: |
      {
        "name": "updated_field"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
