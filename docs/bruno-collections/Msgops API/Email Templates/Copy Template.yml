info:
  name: Copy Template
  type: http
  seq: 7

http:
  method: POST
  url: '{{msgops-api-url}}/email-template/1/copy'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
