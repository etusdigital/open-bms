info:
  name: List Templates
  type: http
  seq: 2

http:
  method: GET
  url: '{{msgops-api-url}}/email-template'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
