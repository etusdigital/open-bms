info:
  name: Sync IP Reputation
  type: http
  seq: 1

http:
  method: POST
  url: '{{msgops-api-url}}/ip-reputation/sync'
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
