info:
  name: Add Label Content
  type: http
  seq: 8

http:
  method: POST
  url: '{{msgops-api-url}}/labels/contents'
  body:
    type: json
    json: |
      {
        "labelId": 1,
        "entityName": "campaign",
        "entityId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
