info:
  name: Create Label
  type: http
  seq: 4

http:
  method: POST
  url: '{{msgops-api-url}}/labels'
  body:
    type: json
    json: |
      {
        "name": "Test Label",
        "description": "Test label description"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
