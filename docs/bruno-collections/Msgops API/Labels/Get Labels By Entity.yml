info:
  name: Get Labels By Entity
  type: http
  seq: 11

http:
  method: GET
  url: "{{msgops-api-url}}/labels/entity/campaign/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
