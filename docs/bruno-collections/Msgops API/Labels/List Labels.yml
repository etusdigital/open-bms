info:
  name: List Labels
  type: http
  seq: 2

http:
  method: GET
  url: "{{msgops-api-url}}/labels"
  params:
    - name: page
      value: "1"
    - name: itemsPerPage
      value: "20"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
