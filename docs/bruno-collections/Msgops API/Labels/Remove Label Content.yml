info:
  name: Remove Label Content
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{msgops-api-url}}/labels/contents"
  body:
    type: json
    json: |
      {
        "labelId": 1,
        "entityName": "campaign",
        "entityId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
