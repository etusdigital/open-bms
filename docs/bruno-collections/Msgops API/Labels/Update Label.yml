info:
  name: Update Label
  type: http
  seq: 5

http:
  method: PUT
  url: '{{msgops-api-url}}/labels'
  body:
    type: json
    json: |
      {
        "id": 1,
        "name": "Updated Label"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
