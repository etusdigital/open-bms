info:
  name: Copy Message
  type: http
  seq: 8

http:
  method: POST
  url: "{{msgops-api-url}}/messages/1/copy"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
