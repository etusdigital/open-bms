info:
  name: Get Email Labels
  type: http
  seq: 10

http:
  method: GET
  url: "{{msgops-api-url}}/messages/template/emails-labels"
  params:
    - name: product
      value: "email"
    - name: language
      value: "pt-BR"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
