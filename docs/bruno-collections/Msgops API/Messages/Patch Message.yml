info:
  name: Patch Message
  type: http
  seq: 6

http:
  method: PATCH
  url: '{{msgops-api-url}}/messages/1'
  body:
    type: json
    json: |
      {
        "subject": "Patched Subject"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
