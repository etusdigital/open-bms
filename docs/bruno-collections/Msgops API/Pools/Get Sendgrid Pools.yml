info:
  name: Get Sendgrid Pools
  type: http
  seq: 7

http:
  method: GET
  url: "{{msgops-api-url}}/pools/sendgrid"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
