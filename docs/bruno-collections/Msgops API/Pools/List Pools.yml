info:
  name: List Pools
  type: http
  seq: 2

http:
  method: GET
  url: '{{msgops-api-url}}/pools'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
