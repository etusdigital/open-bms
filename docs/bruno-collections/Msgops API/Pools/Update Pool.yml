info:
  name: Update Pool
  type: http
  seq: 5

http:
  method: PUT
  url: "{{msgops-api-url}}/pools/1"
  body:
    type: json
    json: |
      {
        "name": "Updated Pool"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
