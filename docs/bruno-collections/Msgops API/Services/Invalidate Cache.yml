info:
  name: Invalidate Cache
  type: http
  seq: 4

http:
  method: POST
  url: '{{msgops-api-url}}/services/invalidate-internal-cache'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
