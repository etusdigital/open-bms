info:
  name: Send Mobile Push
  type: http
  seq: 2

http:
  method: POST
  url: '{{msgops-api-url}}/services/send-mobile-push'
  body:
    type: json
    json: |
      {
        "email": "user@example.com",
        "message": {
          "title": "Test Push",
          "body": "Hello"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
