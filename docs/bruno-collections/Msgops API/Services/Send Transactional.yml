info:
  name: Send Transactional
  type: http
  seq: 3

http:
  method: POST
  url: '{{msgops-api-url}}/services/send-transactional'
  body:
    type: json
    json: |
      {
        "contact": {
          "email": "user@example.com",
          "firstName": "Test",
          "lastName": "User"
        },
        "name": "test-transactional"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
