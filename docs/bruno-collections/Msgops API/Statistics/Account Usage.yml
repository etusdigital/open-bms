info:
  name: Account Usage
  type: http
  seq: 7

http:
  method: GET
  url: "{{msgops-api-url}}/statistics/account-usage"
  params:
    - name: accountId
      value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
