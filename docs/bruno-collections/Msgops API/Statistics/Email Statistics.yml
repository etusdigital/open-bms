info:
  name: Email Statistics
  type: http
  seq: 1

http:
  method: GET
  url: "{{msgops-api-url}}/statistics/email"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
