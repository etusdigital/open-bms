info:
  name: Leads Statistics
  type: http
  seq: 6

http:
  method: GET
  url: "{{msgops-api-url}}/statistics/leads"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
