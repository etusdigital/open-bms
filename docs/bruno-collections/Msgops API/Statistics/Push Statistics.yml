info:
  name: Push Statistics
  type: http
  seq: 4

http:
  method: GET
  url: '{{msgops-api-url}}/statistics/push'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
