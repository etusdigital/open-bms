info:
  name: Copy Segment
  type: http
  seq: 10

http:
  method: POST
  url: "{{msgops-api-url}}/tags/segment/1/copy"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
