info:
  name: Create Segment
  type: http
  seq: 8

http:
  method: POST
  url: "{{msgops-api-url}}/tags/segment"
  body:
    type: json
    json: |
      {
        "name": "Test Segment",
        "conditions": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
