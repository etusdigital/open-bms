info:
  name: Create Tag
  type: http
  seq: 4

http:
  method: POST
  url: "{{msgops-api-url}}/tags"
  body:
    type: json
    json: |
      {
        "name": "test-tag",
        "description": "Test tag"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
