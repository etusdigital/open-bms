info:
  name: Delete Tag
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{msgops-api-url}}/tags/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
