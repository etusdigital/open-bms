info:
  name: Run Segment
  type: http
  seq: 11

http:
  method: GET
  url: "{{msgops-api-url}}/tags/segment/run/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
