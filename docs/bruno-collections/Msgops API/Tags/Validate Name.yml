info:
  name: Validate Name
  type: http
  seq: 7

http:
  method: GET
  url: "{{msgops-api-url}}/tags/validate-name"
  params:
    - name: name
      value: "test-tag"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
