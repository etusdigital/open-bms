info:
  name: Create Automation Test
  type: http
  seq: 4

http:
  method: POST
  url: '{{msgops-api-url}}/tests/automations/create'
  body:
    type: json
    json: |
      {
        "automationId": 1,
        "contacts": ["user@example.com"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
