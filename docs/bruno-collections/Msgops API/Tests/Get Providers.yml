info:
  name: Get Providers
  type: http
  seq: 3

http:
  method: GET
  url: '{{msgops-api-url}}/tests/providers'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
