info:
  name: List Test Results
  type: http
  seq: 1

http:
  method: POST
  url: "{{msgops-api-url}}/tests/list"
  body:
    type: json
    json: |
      [
        { "id": "test-1" }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
