info:
  name: Get Roles
  type: http
  seq: 7

http:
  method: GET
  url: '{{msgops-api-url}}/users/roles'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
