info:
  name: Get User
  type: http
  seq: 5

http:
  method: GET
  url: '{{msgops-api-url}}/users/1'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
