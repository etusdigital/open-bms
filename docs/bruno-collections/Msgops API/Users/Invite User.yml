info:
  name: Invite User
  type: http
  seq: 6

http:
  method: POST
  url: "{{msgops-api-url}}/users/invite"
  body:
    type: json
    json: |
      {
        "email": "newuser@example.com",
        "name": "New User"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
