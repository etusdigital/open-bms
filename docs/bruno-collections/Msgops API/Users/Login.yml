info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{msgops-api-url}}/users/login"
  body:
    type: json
    json: |
      {
        "email": "admin@example.com",
        "name": "Admin User",
        "picture": "https://example.com/avatar.png"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
