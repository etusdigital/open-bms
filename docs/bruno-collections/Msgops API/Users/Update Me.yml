info:
  name: Update Me
  type: http
  seq: 3

http:
  method: PUT
  url: '{{msgops-api-url}}/users/me'
  body:
    type: json
    json: |
      {
        "name": "Updated Name"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
