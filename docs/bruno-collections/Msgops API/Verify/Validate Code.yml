info:
  name: Validate Code
  type: http
  seq: 2

http:
  method: POST
  url: '{{msgops-api-url}}/verify/validate'
  body:
    type: json
    json: |
      {
        "to": "+5511999999999",
        "code": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
