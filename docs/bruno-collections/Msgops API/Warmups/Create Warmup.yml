info:
  name: Create Warmup
  type: http
  seq: 4

http:
  method: POST
  url: "{{msgops-api-url}}/warmups"
  body:
    type: json
    json: |
      {
        "name": "Test Warmup",
        "poolId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
