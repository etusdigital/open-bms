info:
  name: List Warmups
  type: http
  seq: 2

http:
  method: GET
  url: "{{msgops-api-url}}/warmups"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
