info:
  name: Batch Campaigns
  type: http
  seq: 3

http:
  method: POST
  url: "{{send-email-url}}/batch/campaigns"
  body:
    type: json
    json: |
      {
        "campaignId": 1,
        "batchId": 1,
        "contacts": [1, 2, 3]
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
