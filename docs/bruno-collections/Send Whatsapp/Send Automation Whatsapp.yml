info:
  name: Send Automation Whatsapp
  type: http
  seq: 3

http:
  method: POST
  url: '{{send-whatsapp-url}}/automation'
  body:
    type: json
    json: |
      {
        "contactId": 1,
        "automationId": 1,
        "stepId": 1,
        "accountId": 1
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
