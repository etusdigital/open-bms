info:
  name: Add Tag
  type: http
  seq: 1

http:
  method: POST
  url: '{{tag-process-url}}/add'
  body:
    type: json
    json: |
      {
        "contactId": 1,
        "tagId": 1,
        "accountId": 1
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
