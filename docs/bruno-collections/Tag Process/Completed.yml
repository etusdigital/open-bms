info:
  name: Completed
  type: http
  seq: 6

http:
  method: POST
  url: "{{tag-process-url}}/completed"
  body:
    type: json
    json: |
      {
        "contactId": 1,
        "automationId": 1,
        "accountId": 1
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
