info:
  name: Contacts Batch
  type: http
  seq: 4

http:
  method: POST
  url: '{{tag-process-url}}/contacts-batch'
  body:
    type: json
    json: |
      {
        "tagId": 1,
        "contacts": [1, 2, 3],
        "accountId": 1
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
