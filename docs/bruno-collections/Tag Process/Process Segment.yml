info:
  name: Process Segment
  type: http
  seq: 7

http:
  method: POST
  url: '{{tag-process-url}}/process-segment/1'
  body:
    type: json
    json: |
      {
        "segmentId": 1,
        "accountId": 1
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
