info:
  name: Remove Tag
  type: http
  seq: 2

http:
  method: POST
  url: '{{tag-process-url}}/remove'
  body:
    type: json
    json: |
      {
        "contactId": 1,
        "tagId": 1,
        "accountId": 1
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
