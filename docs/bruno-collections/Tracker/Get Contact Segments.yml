info:
  name: Get Contact Segments
  type: http
  seq: 2

http:
  method: POST
  url: '{{tracker-url}}/cs'
  body:
    type: json
    json: |
      {
        "data": "eyJpIjoxfQ=="
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
