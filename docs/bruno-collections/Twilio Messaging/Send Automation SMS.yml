info:
  name: Send Automation SMS
  type: http
  seq: 4

http:
  method: POST
  url: '{{twilio-messaging-url}}/automation'
  body:
    type: json
    json: |
      {
        "contactId": 1,
        "automationId": 1,
        "stepId": 1,
        "accountId": 1
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
