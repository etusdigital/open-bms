info:
  name: Notify
  type: http
  seq: 2

http:
  method: POST
  url: '{{warmup-tracker-url}}/notify'
  body:
    type: json
    json: |
      {
        "warmupId": 1,
        "event": "completed"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
