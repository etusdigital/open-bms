FROM ducktors/turborepo-remote-cache:latest

# GCS auth is handled via Application Default Credentials on Cloud Run
# (the attached service account), so no GCS_* env vars needed at build time.

EXPOSE 3000
